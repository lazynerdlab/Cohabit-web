import {
  CustomModal as Modal,
  CustomInput as Input,
} from "@/lib/AntDesignComponents";

const PropertyModal = () => {
  return (
    <Modal>
      <Input />
    </Modal>
  );
};

export default PropertyModal;
